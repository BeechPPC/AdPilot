{
  "name": "adpilot",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"cd backend && npm run dev\" \"cd frontend && npm start\"",
    "start": "cd frontend && npm start",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "test": "cd frontend && npm test"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
